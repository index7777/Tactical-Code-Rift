import{describe,expect,it}from'vitest';import{battleMusicKey,battleMusicKind}from'./BattleMusicPolicy';
describe('battle music selection',()=>{it('keeps normal and elite encounters on the normal battle track',()=>{expect(battleMusicKind('battle-1')).toBe('normal');expect(battleMusicKind('elite-1')).toBe('normal')});it('selects the boss track only for boss nodes',()=>{expect(battleMusicKey(battleMusicKind('boss-1'))).toBe('boss-battle-music');expect(battleMusicKind()).toBe('normal')})});
