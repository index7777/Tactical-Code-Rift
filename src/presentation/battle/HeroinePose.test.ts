import{describe,expect,it}from'vitest';import type Phaser from'phaser';import{playHeroinePose}from'./HeroinePose';
describe('heroine pose sizing',()=>{it('keeps every source texture on the actor display height',()=>{const calls:{key?:string;width?:number;height?:number}={},sprite={getData:(key:string)=>key==='heroine'?true:96,play:(key:string)=>{calls.key=key},texture:{getSourceImage:()=>({width:846,height:1499})},setDisplaySize:(width:number,height:number)=>{calls.width=width;calls.height=height}}as unknown as Phaser.GameObjects.Sprite;playHeroinePose(sprite,'strike');expect(calls).toEqual({key:'heroine-strike',width:54,height:96})})});
describe('heroine down sizing',()=>{it('keeps the horizontal death drawing from growing to standing height',()=>{const calls:{width?:number;height?:number}={},sprite={getData:(key:string)=>key==='heroine'?true:100,play:()=>undefined,texture:{getSourceImage:()=>({width:600,height:330})},setDisplaySize:(width:number,height:number)=>{calls.width=width;calls.height=height}}as unknown as Phaser.GameObjects.Sprite;playHeroinePose(sprite,'down');expect(calls).toEqual({width:95,height:52})})});
